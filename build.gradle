plugins {
    id 'org.jetbrains.kotlin.jvm' version '1.6.10'
}

group 'com.huskerdev'
version '1.0'

repositories {
    mavenCentral()
}

task compileProject(type: GradleBuild){
    group 'compile'
    tasks = [':native:compile', ':core:moveNatives', ':core:jar']
}